---
const { title, subtitle } = Astro.props;

---

<div class="flex flex-col justify-center mb-10 w-full h-100 align-middle bg-gray-700 items-center bg-[url(/src/images/titlebackground.png)] bg-cover bg-fixed bg-center bg-no-repeat">
    <h1 class="font-bold text-7xl text-text-title">{title}</h1>
    {subtitle && 
        <h2 class="text-4xl text-text-subtitle">{subtitle}</h2>
    }
</div>


